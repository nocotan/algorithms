//#define _GRIBCXX_DEBUG
#include <bits/stdc++.h>
using namespace std;

#define int long long

auto main() -> signed {
    int n, k;
    cin >> n >> k;
    vector<int> v(n);
    for(int i=0; i<n; ++i) cin >> v[i];
    int a = accumulate(v.begin(), v.end(), 0);
    int ans = 0;
    while(true) {
        if(a/)
    }
    return 0;
}

