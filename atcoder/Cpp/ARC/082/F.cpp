//#define _GRIBCXX_DEBUG
#include <bits/stdc++.h>
using namespace std;

#define int long long

int memo[100001];

signed main() {
    int x, k;
    cin >> x >> k;
    vector<int> r(k);
    for(int i=0; i<k; ++i) {
        cin >> r[i];
    }

    for(int i=0; i<k; ++i) {

    }
    return 0;
}

